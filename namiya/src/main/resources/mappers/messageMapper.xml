<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="global.sesoc.namiya.dao.MessageMapper">
	<select id="mInBoxListAll" resultType="Message">
		SELECT
			messagenum,writerid,msg_title,msg_content,regdate,userid,copy
		FROM
			message
		ORDER BY
			messagenum DESC
		WHERE
			copy = 0
	</select>
	
	<select id="mOutBoxListAll" resultType="Message">
		SELECT
			messagenum,writerid,msg_title,msg_content,regdate,userid,copy
		FROM
			message
		ORDER BY
			messagenum DESC
		WHERE
			copy = 1
	</select>
	
	<insert id="mInBoxInsert" parameterType="Message">
		INSERT INTO
			message(
			messagenum,
			writerid,
			msg_title,
			msg_content,
			regdate,
			userid,
			copy
			)
		VALUES(
			messagenum_seq.nextval,
			#{wrtierid},
			#{msg_title},
			#{msg_content},
			sysdate,
			#{userid},
			#{copy}
		)
	</insert>
	
	<insert id="mOutBoxInsert" parameterType="Message">
		INSERT INTO
			message(
			messagenum,
			writerid,
			msg_title,
			msg_content,
			regdate,
			userid,
			copy
			)
		VALUES(
			messagenum_seq.nextval,
			#{wrtierid},
			#{msg_title},
			#{msg_content},
			sysdate,
			#{userid},
			#{copy}
		)
	</insert>
	<delete id="mInBoxDelete" parameterType="int">
		DELETE FROM
			message
		WHERE
			messagenum = #{messagenum}
		AND
			copy = 0
	</delete>
	
	<delete id="mOutBoxDelete" parameterType="int">
		DELETE FROM
			message
		WHERE
			messagenum = #{messagenum}
		AND
			copy = 1
	</delete>
	<select id="mInBoxSelectOne" parameterType="int" resultType="Message">
		SELECT
			messagenum,
			writerid,
			msg_title,
			msg_content,
			regdate,
			userid,
			copy
		FROM
			message
		WHERE
			messagenum = #{messagenum}
		AND
			copy = 0
	</select>
	
	<select id="mOutBoxSelectOne" parameterType="int" resultType="Message">
		SELECT
			messagenum,
			writerid,
			msg_title,
			msg_content,
			regdate,
			userid,
			copy
		FROM
			message
		WHERE
			messagenum = #{messagenum}
		AND
			copy = 1
	</select>
	<update id="mUpdate" parameterType="Message">
		UPDATE SET
			msg_title = #{msg_title},
			msg_content = #{msg_content},
			sysdate,
			userid = #{userid},
			copy = #{copy}
		WHERE
			messagenum = #{messagenum}
	</update>
	
</mapper>